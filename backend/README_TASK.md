# Backend Task

Это учебная версия `backend/` для студентов.

Что здесь специально изменено:
- В `src/main.rs` оставлен `TODO` в функции `to_fixed_6`.
- Один из unit-тестов намеренно сломан и помечен как `TODO(student)`.
- `Cargo.toml` уже смотрит на `../program-task`, чтобы учебные части были связаны друг с другом.

Как запускать:

```bash
cd backend-task
cargo test
```

Ожидаемый результат в начале:
- код компилируется;
- несколько тестов падают;
- после реализации helper-функции и починки теста всё становится зелёным.
